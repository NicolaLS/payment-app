public_base_url: https://reglab.nicolasusca.com

ingress:
  mode: caddy
  tls: acme

lease:
  default_ttl_seconds: 300
  max_ttl_seconds: 1800

evidence:
  retention_days: 14

pools:
  payments-basic:
    default:
      slots: 8
