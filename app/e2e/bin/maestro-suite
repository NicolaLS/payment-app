#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/../.."

# shellcheck source=_reglab-common
source e2e/bin/_reglab-common

load_e2e_env
export_maestro_reglab_env

if [[ $# -eq 0 ]]; then
  set -- flows
fi

maestro test "$@"
