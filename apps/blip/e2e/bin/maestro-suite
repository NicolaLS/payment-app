#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/../.."

# shellcheck source=_blip-e2e-common
source e2e/bin/_blip-e2e-common

load_e2e_env
export_maestro_env

if [[ $# -eq 0 ]]; then
  set -- flows
fi

maestro test "$@"
