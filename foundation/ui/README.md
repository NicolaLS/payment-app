# RAYL UI foundation

This directory is intentionally empty during the repository-skeleton
milestone. UI is promoted here only after at least two implemented apps prove
that its visual and behavioral semantics are identical.
